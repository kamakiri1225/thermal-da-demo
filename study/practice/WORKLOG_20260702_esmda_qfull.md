# 作業ログ: ミッフィー熱源同定を完全逆問題化（2026-07-02）

対象ケース: [A02_miffy_esmda_SCfixTimefix_Q](./A02_miffy_esmda_SCfixTimefix_Q)

HANDOFF に残っていた「次の技術課題」——状態変数を Q_full に変更し、
L⁻¹ の順問題を組み込んだ完全逆問題へ進める——を実装・検証しました。

---

## 1. 出発点: うまくいっていた部分といなかった部分

| 項目 | 状態 |
|---|---|
| 固定センサ温度からの温度場復元（ESMDA） | ○ m=300 で RMSE 19.82 °C |
| センサ数スイープ（10〜400点） | ○ 定量化済み |
| 熱源場の同定 | × `q = -α·L·x` の後付け逆算はノイズだらけ（`fig03_qest_final.png`） |

原因: ラプラシアンは差分オペレータなので、同化後の温度場に残る
セルスケールのノイズを強烈に増幅する。

## 2. やったこと

### 2.1 未コミットのドキュメント同期（コミット `b22b39d`）

6/15 に別PCで行われていた README・HANDOFF 更新をコミット。

### 2.2 完全逆問題版スクリプトを新規実装

[`python/esmda_qfull.py`](./A02_miffy_esmda_SCfixTimefix_Q/python/esmda_qfull.py)

- 状態変数: 熱源場 q（900セル）
- 順モデル: `x(q) = L⁻¹(−q/α)`（splu で一度だけ LU 分解、毎反復は後退代入のみ）
- 観測: 固定センサ位置の温度（温度場版と同じ空間充填配置・同じ σ_r=3 °C）
- 比較用に旧方式（温度場同化 → q 逆算）のベースラインも同梱
- センサ数スイープ（10〜400点 × 3シード）、収束図、比較図、GIF を自動生成

手法解説: [`docs/esmda_qfull_method.md`](./A02_miffy_esmda_SCfixTimefix_Q/docs/esmda_qfull_method.md)

### 2.3 チューニングで見つかった問題と対処

| 問題 | 原因 | 対処 |
|---|---|---|
| 実行が止まる（CPU 4400%） | OpenBLAS が全コアにスレッド生成し空回り | スクリプト冒頭で BLAS スレッドを 4 に制限 |
| R_LOC=3 で3桁発散 | 非局所な順モデルの共分散を切りすぎ | — |
| R_LOC=5 で反復途中に温度 RMSE が 5000 °C 超へ暴走、m=30〜50 では同化なしより悪化 | 局所化更新の遠方への副作用が拘束されない | **R_LOC=10 に拡大**（両方とも解消） |

教訓: 順問題が非局所な逆問題では、局所化半径を
観測感度の広がりに合わせて広く取る必要がある。

## 3. 結果

### 3.1 数値

| 指標 | 値 |
|---|---|
| 温度場 RMSE（m=300, x(q_mean)） | 20.06 °C（温度場直接同化: 19.82 °C と同等） |
| 熱源 q の RMSE | 38.8 で飽和（q_true の std=40.0） |
| corr(q_est, 平滑化 q_true), m=30/100/200/300 | 0.33 / 0.59 / 0.65 / **0.76** |
| 旧方式 q_derived の同相関（m=300） | 0.74 |

### 3.2 わかったこと

1. **セルスケールの熱源エッジは点温度観測から原理的に復元できない**。
   q_true は輪郭線状のエッジ場で、L⁻¹ の平滑化作用により高周波情報が
   観測に残らない（逆問題の不適切性）。N や反復数を増やしても改善しない。
2. **平滑化スケール（σ=1）なら相関 0.76 で復元でき、目・鼻・輪郭が視認できる**
   （`fig08_qfull_vs_derived.png`）。
3. **旧方式との差は「情報量」ではなく「使いやすさ」**。線形問題なので
   取り出せる低周波情報は両者ほぼ同じ。ただし旧方式は信号が増幅ノイズに
   埋もれて図として読めず、Q-state は事前分布が q を正則化するため
   そのまま使える滑らかな熱源場が出る。

### 3.3 生成物（`A02_miffy_esmda_SCfixTimefix_Q/img/`）

- `fig05_qfull_rmse.png` — RMSE 収束（旧方式の水準線付き）
- `fig06_qfull_qest_final.png` — 最終推定熱源場（m別）
- `fig07_qfull_xrecon_final.png` — 推定熱源からの順解析温度場（m別）
- `fig08_qfull_vs_derived.png` — 真値 / 平滑化真値 / Q-state / 旧方式の4面比較
- `fig09_qfull_sensor_count_sweep.png` — センサ数スイープ（q・温度）
- `anim_esmda_qfull.gif` — 同化過程アニメーション
- `docs/sensor_count_sweep_qfull.csv` — スイープ数値

## 4. 次にやるなら

- 非定常問題化（時系列観測で熱源の時間変化を追う）
- エッジを表現できる事前分布（非ガウス・全変動正則化系）の検討
- OpenFOAM / FrontISTR ケース（sample/002-1 系）への Q-state 同化の展開
